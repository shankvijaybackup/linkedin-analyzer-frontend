# Local fallback only — usually overridden by .env.local
REACT_APP_API_URL=http://localhost:3001

# Do NOT uncomment the below line in production builds — set via Vercel UI
# REACT_APP_API_URL=https://linkedin-analyzer-backend-rk6a.onrender.com
